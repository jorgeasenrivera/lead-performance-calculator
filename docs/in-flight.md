# In flight

The board. One row per item, and a row is claimed by committing it to `main`
before the work starts. That commit is the lock: it needs no shared runtime and
it survives an agent losing its session. If two claims race, the one already on
`main` wins and the other agent takes something else.

Release a claim in the same pull request that finishes the work, by setting the
row to done and adding the pull request number.

**Rules that live on this board**

- Never work an item claimed by somebody else, and never push to their branch.
- At most one open branch may touch `src/LeadPerformanceCalculator.jsx` or
  `src/Manager.jsx`. Check the Files column before claiming.
- If the hot files are taken, take an item under `api/`, `scripts/`,
  `supabase/`, `test/`, `workers/`, `native/` or `docs/`, or review the open
  pull request instead.

**The ids carry the prefix of whoever wrote the row.** `C` for Claude, `X` for
Codex, each counting up its own run: `C1`, `C2`, `X1`. Pick the next free number
in **your own** letter and you cannot collide with the other agent, because
nobody else is drawing from it. Jorge settled this on 15 September.

The prefix is the row's author, not the item's owner. A row Claude writes for
Codex to own is still a `C` row, because the point is the moment of writing: two
agents both reaching for "the next free number" in one shared run is the race,
and an owner prefix does not fix it when one agent opens an item for the other.
The Owner column says who has it.

This was learned the hard way, twice in one afternoon. Codex claimed A7, Claude
claimed A7 two minutes later, and then both agents fixed it at once and made a
second collision: Claude dropped its row, Codex renamed its own to A8, and
Claude wrote a new row at A8 having read the board before Codex's rename landed.
A read, a write, and somebody else's write in between. A shared counter needs
coordination that a commit race does not provide.

If two rows ever do take one id, the row already on `main` keeps it and the
later push moves. Renumber your own row, never the other agent's.

The Done table is keyed by pull request number for the same reason: it is
already unique, and an invented id there was one more counter to race on.

Status is one of: `open`, `in progress`, `needs approval` (a visual change
waiting on Jorge's proposal page), `in review`, `done`.

---

## Open

| # | Item | Owner | Branch | Files | Status | PR |
|---|------|-------|--------|-------|--------|-----|
| C1 | **The Online room.** The under construction page is built and live. The room itself still exists: its tool pill, sign-in parameter, salesperson tab, `LEAD_VARIANTS` entry and row family. **Parked indefinitely on 16 September, and this is not the same as LATER.** Jorge has an idea for what the room should be, and wants to see how much the tool gets used for that before spending anything on it. So the plumbing stays: removing it would have to be undone, and it costs nothing but its own weight while it sits. Worth knowing when that decision comes: **nothing measures this today.** There is no usage recording anywhere in the app, so "how much is it used" has no data behind it and will need something to record it first, or a judgement call instead. Do not propose removing the room, and do not build measurement for it unasked. | - | - | `src/Manager.jsx`, `src/LeadPerformanceCalculator.jsx` | parked, Jorge's call | - |
| C2 | **Ten stores with no goal.** Four of fourteen have set one. **This row used to say the prompt was not built, and that was wrong.** `StoreHero` already offers `/ set a goal · last month N` on a month with none, already turns `/ N goal` into a button titled Change this month's goal, and already writes an audit line on every set. Every admin and manager sees it. What is missing is that nothing *asks*: the control waits to be noticed. And the audit line carries the new figure but not the old, with no confirmation, so an accident and a decision read the same. Jorge chose **stays** and **both** on 15 September, and it is built and pushed. One finding changed the shape: `storeGoalFor` falls back to the store's standing figure, so a card keyed on "this store has no goal" would never appear again for the four stores that have ever set one. `goalMonthState` tells a month's own figure from one it carried. The carried-over wording is new and still needs Jorge's word. Settled 15 September: the card **stays**, the audit gets **both** halves, and goals are **month specific with the figure written in**. The standing-figure fallback in `storeGoalFor` is gone, along with the two screens that had inlined it by hand. Four store-months that were borrowing a goal now correctly show none: Holler Ford July and August, Holler Honda July, Driver's Mart Winter Park July. | Claude | `claude/mobile-site-optimization-qvei7u` | `src/Manager.jsx`, `api/_store-month.mjs` | done | #343 |
| C4 | **The desks caption on the Live Activity.** It reads "1 and 2 and 3 and 4 and 5 and 6 are open" because the free desk numbers are joined with the word "and". It should say "6 desks open". Written and held: there is no Swift toolchain in a web session, and the pull request's own iOS check could not run because the Expo account has used its free-plan builds for the month. It merges when a real build can check it. The patch is on `claude/desks-caption`. | Claude | `claude/desks-caption` | `native/targets/queue/QueueActivity.swift` | done | #340 |
| X1 | **Make the required checks reliable on Windows.** Git currently converts the checkout to CRLF, which breaks source guards written against LF, and five tests turn a Windows file URL into `C:\\C:\\...`. Keep code files on LF and resolve file URLs with Node's cross-platform helper so Codex can run the same required checks Claude and CI run. No product behaviour or pixels change. | Codex | `codex/windows-checks` | `.gitattributes`, `test/` | done | #336 |
<!-- Claimed on 16 September as C19, C20 and C21, which collided with the
     casted TV board's C19 below. Renumbered after the merge, so #354, its
     commit and the proposal page all still say C19 to C21. Same three rows. -->
| C22 | **Every screen with the room bar scrolls by exactly the reserve, with nothing in the band.** `.lpc:has(> .ar-bar) .q-page{ padding-bottom:72px }` (14837) adds the bar's reserve *below* two boxes that each already claim the whole viewport: `.sf .q-stage{ min-height:var(--dvh) }` (15049) and `.sf-live{ min-height:var(--dvh) }` (15426). Measured on Live Floor at 390x844 with a line of eight: scrollHeight 916, clientHeight 844, over by 72, which is the reserve to the pixel. At Largest it is over by 116. Jorge, on 16 September: Live Floor is not meant to be a scrollable page. Reported on every screen, in Holler Ford's test account. | Claude | - | `src/LeadPerformanceCalculator.jsx` | done | #354 |
| C23 | **The room bar is the only bottom-anchored thing reading raw `env()`.** `.ar-bar{ bottom:calc(env(safe-area-inset-bottom, 0px) + 14px) }` (14693). `.help-fab` (14167), `.mc-pill` (15995) and the board's padding (15994) all read `--sab`, which is `max(env(...), var(--shell-inset-bottom))`. Two costs. Android: the shell's own comment (`native/App.js:164`) says Android's WebView cannot read `env()`, which is why it injects `--shell-inset-bottom`, and the bar ignores it, so it sits over the gesture bar. Text size: `.ar-bar` is one of the four zoom roots (16129), so raw `env()` is multiplied by the zoom, which is the fault #334 fixed everywhere else. Measured: a 34px shell inset moves the bar not one pixel, and the gap below it goes 14px at Normal to 18.2px at Largest. | Claude | - | `src/LeadPerformanceCalculator.jsx` | done | #354 |
| C24 | **The Live Activity crops because the card holds more than the lock screen allows.** Jorge, 16 September: the card is a fixed size and our elements are too big for it. Measured from the layout: two lanes with neither urgent is about 233pt of content (floor lane 96, divider 1, phone lane 136, where the phone lane carries a 38pt cord *and* a 20pt desk row because `state == "free"` satisfies both conditions at once, lines 943 and 946). A big lane with buttons plus a small lane is about 217pt. Apple's lock screen allowance is 160pt. What to drop is Jorge's call. | Claude | - | `native/targets/queue/QueueActivity.swift` | done | #354 |
| C25 | **The ground behind the rooms: a parallax backdrop.** Approved by Jorge on 17 September, option B on the research page, after four rounds. Blobs, each its own layer at its own speed from a tenth of the thumb to four tenths; far ones bigger, dimmer, hazier and slower, near ones smaller, brighter, sharper and faster; each starts its colour journey after the one before, so they never turn together. Haze from gradient stops rather than a filter blur, which would run every frame of a drag on a phone. Whole pixel transforms, because sub-pixel dots shimmered. The journey is built on the store's own tab list (`api/_rooms.mjs` plus the seven lines at 7576), so it holds for floor only (two tabs, the default and the common case), floor and line (three), line only (one tab, no bar, no transition) and neither. **Cost, stated and accepted:** the rooms stop being opaque toward the bottom, which repaints the bottom of every salesperson screen at rest, not only mid swipe. Every screen to be walked at all three text sizes before merge. | Claude | `claude/ground-parallax` | `src/LeadPerformanceCalculator.jsx` | done | #356 |
| C26 | **The corner gets its own colour, and the parallax starts quiet.** Jorge on 17 September, after C25 shipped. Home was `#15211B` against the floor's `#070A08`, almost all brightness and hardly any hue, so it was the one swipe with nothing to travel: it is warm sand now, `#1B1A14` with the app's own `#E4C98D` family on its blobs, so the journey runs warm, green, blue. And the travel is squared rather than linear, so a small drag barely disturbs the ground and a full one arrives at the whole effect, which is the multiplane at the top of the research page. Speeds raised to a spread near the multiplane's own, 0.12 / 0.30 / 0.55 behind a room travelling at 1. The light corner stays out, as before. | Claude | `claude/home-sand-ramp` | `src/LeadPerformanceCalculator.jsx` | done | #357 |
| C27 | **The dots come out of the corner too.** Jorge on 17 September: he likes B's colour blending and wants dots behind the swipe, a shade more transparent than the demo's, because the corner is already dotted. It was: `.mc-aurora u`, 22px in white at `.16` over half opacity, inside the room and therefore unable to travel. Three fields now on the backdrop, not one, at `.07`, `.08` and `.10` on 22, 34 and 54px grids with a falloff wider than a pixel so it does not shimmer as it moves, masked top and bottom so it never reaches the head or the bar. It is the furthest thing on the screen at `0.07`, slower than any blob. The corner stops drawing its own inside the stack; its coloured lights stay. | Claude | `claude/ground-dots` | `src/LeadPerformanceCalculator.jsx` | done | #358 |
| C28 | **Two rooms readable through each other on every cross.** Mine, from #356: making both sheets transparent is safe for a DRAG, where they tile, and wrong for a CROSS, where they overlap. Jorge photographed it on 17 September, "The floor isn't open yet" and "The line isn't open yet" printed through one another, and a committed swipe runs the cross too so it is not only a button. Both sheets take the arriving room's ground for the length of it: the same colour on both is what keeps it from being a seam, opaque is what keeps them from being read through. The guard that should have caught it only checked for two class names, a proxy for the property rather than the property; it checks the real thing now. | Claude | `claude/cross-opaque` | `src/LeadPerformanceCalculator.jsx` | done | #359 |
| C29 | **Home to Live Floor should follow the thumb like the others.** Jorge, 17 September: all three should be swipes. They are two tabs of ONE sheet today, the same `.q-page` with different classes and content, so there is no second sheet to pull and that pair commits on lift. The gesture's own comment has called this out as its own job since #351. Rendering the corner and the floor as two sheets is the change. Proposal out 18 September, four decisions (the shape, how the corner arrives, the pills, when you are up): https://claude.ai/artifact/QviTxYczeWc3CKKJ7zPePt. Recommended shape is two panes on one track inside the floor page rather than three real sheets, because the floor page's thousand lines of state stay where they are. Decided the same day, all four as recommended: two panes on one track inside the floor page (S1), the travel is the corner's arrival (S2), the pills follow the thumb (S3), You're up snaps to the floor (S4). Built in #389. Merged on Jorge's call of 19 September without the phone check, this one time: the preview cannot sign a salesperson in (its functions have no server key, see C73), so the phone check is on the site. | Claude | `claude/two-sheets` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs`, `scripts/feel.mjs`, `docs/sheets/rooms.html` | done | #389 |
| C30 | **The corner goes back to green.** Jorge, 17 September, on the colours fighting: the corner carries four large coloured lights of its own (`.mc-aurora i`, three of them green) which sit in FRONT of the ground, so the warm sand from #357 read as two ideas at once. Warming those lights to match was the other way out; he chose this one. The ground is `#15211B` again and nothing is left of the sand. The squared ramp from #357 stays. | Claude | `claude/corner-green` | `src/LeadPerformanceCalculator.jsx` | done | #360 |
| C31 | **The backdrop costs 221.5 MB of GPU texture and has to be rebuilt on a canvas.** Measured on the built app at 390x844 and dpr 3: seven layers, each 280vw wide, 31.6 MB apiece, against the 22.6 MB the app used before any of this. That one number explains the lag, the dots arriving late and the black between rooms, and it never showed on a laptop, where the median frame through a swipe was 16.7 ms. One canvas the size of the screen at 1x is 1.3 MB: the oversizing goes because a pattern wraps, the resolution drops because soft gradients do not need 3x, and it stops working entirely at rest. Jorge chose on 17 September to leave the current one live and rebuild at speed rather than revert. Also to fix while in there: two effects with no dependency list, and the ground layer having no colour until the first frame of JS, which is the black. | Claude | `claude/ground-canvas` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #361 |
| C32 | **Three from a photograph of the phone: the flashing between pages, the dots' quality, and the seam.** All three measured on the built app rather than reasoned about. The flash is not memory and not a blank canvas: `.ar-stack.x` from #359 painted an opaque flat ground on BOTH sheets for 440 ms and then snapped to the backdrop in one frame, measured as `rgb(6,9,15)` held then `rgb(25,104,126)`. The leaving sheet is cut to where the arriving one has reached instead, so nothing overlaps and every sheet stays transparent. The dots were soft because I drew them at one backing pixel and wrote that it would not show; it showed. They get their own canvas at the screen's resolution, 12.56 MB of backdrop at dpr 3 against the 232.8 the seven layers were. The seam is the loading curtain's `box-shadow:0 0 60px`: parked, a curtain sits one screen left, so the only thing that shadow reaches is the first 60px of the room it is parked in front of, which during a drag is the join. Same fault as #356's arriving-sheet shadow, in a second place I missed. | Claude | `claude/cross-and-dots` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #362 |
| C33 | **No light crosses the rooms on a switch.** Jorge, 18 September: every swipe looked like a white whoosh coming from that side, and the parallax should show the movement instead of a screen wipe. The sheen was `.ar-room.ar-in > .q-page.sf::after`, a band of white at .13 riding across the arriving sheet on the wipe clock. His argument is the better one: three dot fields and three lights at six speeds behind the rooms already say a room moved, and a band crossing the screen on top of that says it again in the language of a wipe. Two answers to one question, and the weaker one was the loud one. | Claude | `claude/no-sheen` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #363 |
| C34 | **The corner's elements arrive from the right, not from below.** Jorge, 18 September: coming off the Live Floor they lifted as they loaded, and every element should swipe right to left subtly instead. It was `.mc > *` on `mcRise`, a 14px `translateY`. A lift says the screen was built; a slide says it arrived, and off the Live Floor the second one is what happened. Same curve, same clock, same stagger, only the axis. Two things found while in there: `mcRise` was declared twice and the later one silently won for both users, and `.mc > *` had no reduced-motion case. Both fixed. | Claude | `claude/slide-and-no-sun` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #364 |
| C35 | **Sunlight is removed.** Jorge, 18 September. A second set of colours for the floor and the line, deep green with cream on it, behind a switch on the corner. Out: the switch, the boot effect that set `html.sun`, the stored preference, the glyph, and twenty-two rules. The two rules that existed only to keep the backdrop out of daylight's way are unscoped rather than deleted, so what daylight was excepted from now always holds, and nothing switches the backdrop off any more. The **Look** control, auto, dark and light, is a different thing and is untouched. | Claude | `claude/slide-and-no-sun` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #364 |
| C36 | **A new build reaches the phone a launch late, which is why two shipped changes looked broken.** Jorge, 18 September: the whoosh and the rise were still there on his phone while being absent from the live bundle. Not a second source: the service worker only ever let a new build in on the way to the BACKGROUND, so the reload happened off-screen and the build a person saw was always the one before last. A force quit sends no backgrounding, so force quitting made it worse rather than better. A build is now taken before the first touch of a fresh open, and it is LOOKED for rather than listened for: measured, the new worker is installed and waiting 441 ms after a reload, which can be before `register()` resolves, so a listener attached afterwards never hears it. | Claude | `claude/sw-and-sched-card` | `src/main.jsx`, `src/sw.js`, `test/feel.test.mjs` | done | #365 |
| C37 | **The month goes on a card.** Jorge, 18 September: the dots in the background interfere. A grid of small dots was being read against the backdrop's three fields of them, which was fine when the ground was flat and is not now. It carries `.mc-card` rather than copying it, so the light corner gets its version too. | Claude | `claude/sw-and-sched-card` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #365 |
| C38 | **No question mark in the Phone Line.** Jorge, 18 September. The help button was hidden on the corner and the floor, which both carry `.mc-shell`, and the line does not, so the line was the only screen in the stack still showing one. The way to help is the row in the You sheet, which every room reaches. | Claude | `claude/rooms-polish` | `src/LeadPerformanceCalculator.jsx` | done | #366 |
| C39 | **The two rooms' words sit at the same height.** Measured with both rooms shut: the floor's header 638px tall with its title at y=365, the line's 710 with its title at 401. The floor took C22's 72px reserve out of its height and the line did not, so the line's column ran taller and its centred words sat 36px lower. The line takes the same reserve now, which also removes its empty band. | Claude | `claude/rooms-polish` | `src/LeadPerformanceCalculator.jsx` | done | #366 |
| C40 | **The corner lands rather than appears, and the spine lands with it.** 22px on a curve that spends almost all of itself slowing down, against 14px on an even ease. The spine had been excluded alongside the two things that genuinely cannot move. | Claude | `claude/rooms-polish` | `src/LeadPerformanceCalculator.jsx` | done | #366 |
| C41 | **A swipe finishes where the thumb left it.** The release threw away the drag's position and restarted the cross from the tap's 26 per cent: measured, the leaving sheet jumped from -250px to 0 and the arriving one from 143 to 102 in a single frame. The same frame put the leaving sheet's title inside the cut, which is Jorge's peek of letters near the edge. One fault, both symptoms. A swipe now tiles to the end the way it ran under the thumb, and tiling needs no cut. | Claude | `claude/rooms-polish` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #366 |
| C42 | **The other room is built once the first has settled.** Building a room is what starts its fetch, and the wait was a flat 2.5s where what was meant was "once the first has settled", so a person crossing early landed on the `sf-loading` skeleton for most of a second. Measured at a dealership's lag: a swipe 100ms after the bar appears used to show it and does not now. | Claude | `claude/rooms-polish` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #366 |
| C43 | **One motion for a tap and for a swipe.** Jorge, 18 September: pressing a button looks like the pages fold on top of each other rather than the side to side of a thumb swipe. A tap was a push, the arriving sheet coming in from 26% over the one being left, which is exactly what folding looks like. Both now tile: one sheet ends where the other begins, same distance, same curve, and the only difference is where they start. The push, its two sets of cut keyframes and the flat slab before them are all gone rather than left unused. | Claude | `claude/tap-travels` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #368 |
| C44 | **The app opens at Home.** Jorge, 18 September. It opened on the room you were last in, which is a reasonable default for a tool somebody lives inside and the wrong one here: the corner is where the day is read. LAST is still offered and anybody who has chosen it keeps it. | Claude | `claude/tap-travels` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #368 |
| C45 | **Home and Live Floor travel.** They are two tabs of one sheet, so switching moved nothing at all: measured, one sample, both sheets at 0 and no animation running, which is Jorge's "the front elements are just snapping". The sheet cannot travel because there is only one; its CONTENTS can, and now come in from the side the bar moved on the rooms' clock and curve. The one being left does not slide out with it, because there is only ever one of them. **A step towards C29, not C29.** | Claude | `claude/tap-travels` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #368 |
| C46 | **The first open after a deploy took 3.2s against a normal 2.0.** Jorge, 18 September: it is taking a long time to initially load in. Not the app: steady-state boot measured flat back to #353, before the backdrop work, at 1940 to 2012 ms. It is #365's doing. Adopting a build means reloading, and `location.reload()` is held until the page it was called on finishes loading, so a build taken 266 ms into an open did not commit until 1301 ms and threw the whole boot away. Mid-session adoption is gone. A build waiting from last time is now taken before a pixel is drawn, and one that installs during an open is left for the way out or the next open. Late by one open at worst, never slow. | Claude | `claude/faster-update` | `src/main.jsx`, `src/sw.js`, `test/feel.test.mjs` | done | #369 |
| C47 | **The corner's cards no longer flash in on top of the slide.** Jorge, 18 September: the elements on the home card appear, it flashes, and the landing has less inertia than the other pages. Two motions were stacked: the stage sliding in on the rooms' curve, and every card fading from transparent and easing 22px on its own curve at the same time. Measured: the page 90 per cent off screen with its cards at half opacity, then the cards still settling 500 ms after the page had stopped. A corner that arrives on the slide now keeps its cards still, and reads how it arrived once at birth so nothing restarts when the slide's class goes. The cold open keeps its entrance. | Claude | `claude/corner-lands` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #370 |
| C48 | **A 30-second open after a force-close: not reproduced, and the update path hardened against every way it could happen.** Jorge, 18 September. Four scenarios on the worker live on the phone, clean deploy, slow lot, background-then-reload, background-kill-reopen, and none stalled; the one 30s seen was my own probe waiting for a tab bar on a signed-out page. What was found and is real: the takeover listener lived inside `register().then` and a takeover can land 18ms into a page, before it exists, leaving the page a build behind in silence; the previous build's cache was deleted the instant a build was taken, which since #369 is mid-load of a page served from it; every open waited unboundedly on `caches.open`, which WebKit can stall after a process kill; the pre-draw swap could loop; and my own `res.clone()` ran after the body was handed back. All five bounded, each measured. | Claude | `claude/keep-last-cache` | `src/main.jsx`, `src/sw.js`, `vite.config.js`, `test/feel.test.mjs` | done | #371 |
| C49 | **The room that sat on its curtain for eight minutes, polling successfully the whole time.** Jorge, 18 September, with a screenshot: force close, reopen, and Home sits on the Sage mark; "as of 8 min ago" in the corner. The project logs for that window show the phone reading the floor row every five seconds and getting 200 every time. What was found: a first read slower than the 5 s timeout is thrown away by the timeout but keeps running, and when it finishes it records the row's stamp. Every poll after that asks for the stamp, gets the same one, answers "same", and the room, which never received the row, holds its curtain for ever. After a force-close the first read waits behind the token refresh, which is how a fast lot goes over 5 s. Fix: a read records the stamp only when its answer is used; and a room with nothing on screen asks for the whole row. The corner pill was also frozen: it read the network from a snapshot taken at the last offline flip. | Claude | `claude/stamp-after-answer` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #372 |
| C51 | **The "You're up" takeover stops 72 px short on the Live Floor tab.** Jorge, 18 September, two screenshots: on Live Floor the takeover ends above the foot of the screen and the dark ground shows through under the bar; on Home it fills. The floor tab's stage is 72 px shorter than the screen on purpose, to clear the bar (the line got the same in C45), and the takeover is sized to the stage. The card sits 36 px lower on Home for the same reason, which read as the card moving when the connection bar came and went. The takeover takes the whole screen on both tabs now, so both look like Home does. | Claude | `claude/uptake-full` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #374 |
| C52 | **The corner's Closing sheet is two decimal places out, and the line clashes with the ground.** Jorge, 18 September, two screenshots: 6 of 55 shown as 0.11%, 4 of 24 as 0.17%, 9 of 36 as 0.25%. The month stats carry the channel rates as fractions, the way the manager's side formats them, and the corner printed the fraction with a percent sign after it. And the line on Home, the rail the light runs along to the door, sits on a 7 per cent white over the dotted ground, so the light's dots and the ground's dots read as one field. The rail takes a dark, solid ground. | Claude | `claude/corner-closing` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #375 |
| C53 | **The Live Activity's glyphs: no glow.** Jorge, 18 September, with a screenshot of the island: some icons glow and some do not, and it should be none. The handset glyph glowed when lit and the big glyphs never did; your pip glowed and the cord's pips carried a shadow. All of it off; colour and size say lit. **This row first said the dots were drawn at the wrong size, and that was wrong**: the glyph draws them at 0.96 of a cell, which is the site's radius of 0.48 exactly, so the island glyph matches the site and is only small. Swift, so not compiled here. **I said this needed a TestFlight build the board owed, and that was wrong**: the Sage app workflow builds iOS and uploads to TestFlight on every merge that touches native/, and this went out as build 38 on 18 September. | Claude | `claude/island-glyphs` | `native/targets/queue/QueueActivity.swift` | done | #376 |
| C54 | **The Live Activity crops again when a lane is hot.** Jorge, 18 September, with a lock screen: the phone lane leading with Desk 1 is yours, its cord, its desk row and Take it, the floor lane under it, and the headline cut off at the top. C24 shrank everything and said plainly it was still over 160; a hot lane with buttons over a small lane with a rail is about 170 to 200. The rule now: while one lane is hot, the other is one line, and the hot lane shows the one picture its message needs (the desks on an offer, nothing under You're up). Swift, not compiled here. Went out as TestFlight build 39 on 18 September, by the workflow; the rows that said a build was owed were wrong. | Claude | `claude/activity-hot-fits` | `native/targets/queue/QueueActivity.swift` | done | #377 |
| C55 | **The rail on Home runs in from off screen, and the month card's goal caption goes.** Jorge, 18 September, with screenshots. The rail's left end sat exactly on the screen's edge, so the light's dots started at the edge rather than arriving from beyond it; it starts past the edge now, and the page clips it there. And the line under the month chart, "GOAL 40 · 20 TO GO · 4 BEHIND", said in words what the pace line, the goal ring and the pace pill already draw; gone, the two dates stay. | Claude | `claude/rail-spill` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #378 |
| C56 | **Location is asked for once, at first open, and never again at the door.** Jorge, 18 September: the app should ask for location at install, always, so it is not asking every time somebody joins the line. The shell already holds expo-location and asks only when a lot fence is set; the page reads the WebView's own geolocation, and it is the WebView that asks at every join. Now the shell asks for when-in-use and then always at first launch, and answers the page's position requests itself, so inside the app the WebView never asks. In a browser the page reads as before. The shell's side went out as TestFlight build 40 on 18 September, by the workflow. | Claude | `claude/location-once` | `native/App.js`, `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #379 |
| C57 | **A room opens itself at the store's time, per weekday.** Jorge, 18 September: the line should open at a set time on a schedule rather than a manager opening it, because the floors open at different times on different days. The day's row is what "open" means and only the wall made one; now the store carries a time per weekday per room (blank: the desk opens it, as before), the first phone that looks after the time asks `/api/open-room`, which makes the wall's row once, and the room says OPENS AT 9:00 AM while it waits. | Claude | `claude/rooms-open-on-time` | `api/_rooms.mjs`, `api/open-room.mjs`, `api/_store-keys.mjs`, `src/LeadPerformanceCalculator.jsx`, `src/Manager.jsx`, `test/` | done | #380 |
| C58 | **Jorge's decisions on the Closing sheet and the floor's buttons.** From the proposal of 18 September: the standard on every bar, graded the way the wall grades (A1); the opened channel shows the month in figures, the thirty-day line with the standard on it, and the move since the last import, in place of the sentence at the foot (A2); one line of coaching from the standard (A4, a now); with a customer no Here, Lunch or Away (B1); the table as one line under the big button in the room's ink (B2); FlyBy and T.O. only with a customer (B4). Declined: new and used (A3) and the pair reshaped (B3). | Claude | `claude/decisions-18-sep` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #381 |
| C59 | **The activity's rail light funnels in to you, and the rail stays on You're up.** Jorge, 18 September, from the lock screen: the dots should funnel from the left to the icon, the way the app's light does. The app's light is three dots running in from the edge with a stop at every person; a Live Activity cannot run a loop, so the widget draws that light at rest, and it drew it as an even dotted line edge to you, which is a track, not a light. Now the dots close up, grow and brighten as they arrive at your pip. And the hot floor lane keeps its rail (C54 had dropped it to fit) and gives up its caption instead: 117 pt with the strip under it, 153, under 160. The hot phone lane keeps a small cord the same way. Swift, not compiled here; the workflow builds it on merge. | Claude | `claude/rail-funnel` | `native/targets/queue/QueueActivity.swift` | done | #382 |
| C61 | **Every dot in the activity's glyphs is the same size.** Jorge, 18 September, from the island: certain dots bigger than others. At 12 points on a 3x screen a 7-cell glyph has 5.14-pixel cells and 4.9-pixel dots, so some rasterise at 5 pixels and some at 4. The grid is snapped to whole pixels now: a cell is a whole number of pixels, every dot is that size, and the grid sits centred in the frame it was given. Swift; compiled on the pull request by the label. | Claude | `claude/pix-snap` | `native/targets/queue/QueueActivity.swift` | done | #384 |
| C62 | **Three glitches on the phone between Home and Live Floor.** Jorge, 18 September, five screenshots. Tapping Home from Live Floor: the corner sits mid-slide, the whole stage still offset left, then snaps into place, because the slide class was taken off by a fixed timer that fires before the animation has finished on a phone that started it late; it ends on the animation's own end now, with a long backstop. Swiping to Live Floor from the foot of Home: a blank screen for a beat and the floor coming down from the top, because the two tabs share one scroll box and the floor arrived scrolled where Home was; each tab keeps its own scroll now. The ground changing colour a moment after arriving: measured on the built app, see the pull request. | Claude | `claude/tab-glitches` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #385 |
| C63 | **Why the back-and-forth, and the four things that stop it.** Jorge, 18 September. Two state sheets, decided once and then held to: the Live Activity card, every lane by hot or quiet by phase with its height, and the rooms' transitions, tap, swipe and tab, what animates and what holds. Two rules in AGENTS.md: a visual change on the phone is not merged until Jorge has had it on a phone, and phone findings are batched into one pass, one build, one test session a day. And the feel harness runs in Playwright's WebKit as well as Chromium, in CI, because WebKit is what the app's WebView is. | Claude | `claude/less-back-and-forth` | `AGENTS.md`, `README.md`, `scripts/feel.mjs`, `.github/workflows/checks.yml`, `docs/` | done | #386 |
| C64 | **The first batch under the new rules: Jorge's decisions on the two sheets.** 18 September. D4: a desk free draws the desk row only, never the cord as well, on the card. R2: each room keeps its own scroll; the memory read window.scrollY and the page is its own scroll box, and a hidden room loses its scroll besides, so it is saved from the room's page before the cross and put back after. R4: **nothing to change, and the sheet's finding was wrong**: the ground follows the thumb only on a real swipe between the floor and the line, which is what R4 asked for; a tap's blend cannot be held by a touch. Corrected on the sheet. Also the feel harness's press step, which drove touches through a Chromium-only channel and killed the WebKit run at that step; it dispatches touch events in the page now, in both browsers. Not merged until it has been on a phone. | Claude | `claude/decisions-batch` | `native/targets/queue/QueueActivity.swift`, `src/LeadPerformanceCalculator.jsx`, `scripts/feel.mjs`, `docs/sheets/`, `AGENTS.md`, `test/feel.test.mjs` | done | #387 |
| C65 | **The Board's TV keeps going white.** Jorge, 18 September. app_errors: eleven "Failed to fetch dynamically imported module: Manager-<hash>.js" from the Driver's Mart wall today, in pairs, one pair per deploy, including on the build it had just reloaded onto. Three things: every board commit deployed production, so the wall reloaded for a one-line docs change (Vercel now skips a deploy that touches only docs and markdown); the worker keeps the last two builds' caches since #371 but looked assets up in the current one only, so a page on the previous build asking for its own chunk after a takeover got a 404 instead of the copy it had (it looks in every kept cache now); and the boundary held the screen as null, which is the white (it holds the board's ground now). | Claude | `claude/tv-white` | `src/sw.js`, `vercel.json`, `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #388 |
| C66 | **WebKit says the rooms and the taps are over the bar, and the harness was measuring itself.** Opened 18 September from the first four WebKit feel runs. Read to the measurement on 21 September: two real findings and one correction. One, the month chart's caption fitted itself in a layout effect with no dependency list, so every render of that card flushed layout for the whole document, which mid cross is both rooms: 8 forced layouts and 38 ms in WebKit against 9 and 12 in Chromium. Fixed in #400, which took it to 4 and 23 and moved the cross's own clock from 79 to 88 ms down to 74 to 79. Two, the tap rows were timed with a stopwatch outside the browser, and Playwright's click waits for the element to be stable across consecutive frames first, which on WebKit's long frames cost two of them: the same tap read 116 to 196 ms through the driver and 15 to 38 dispatched in the page. The rows are timed on the page's own clock now and the bars are set from what both engines actually do. The correction: on 21 September I named the pips, then the animations, from single taps on a shared runner where one condition spread 19 to 137 ms. Repeated four times a condition, neither was distinguishable from noise. What is left of the original complaint is C79, and it is not a CI question. | Claude | - | `scripts/feel.mjs`, `.github/workflows/checks.yml` | done | #400, #401 |
| C67 | **A checked-in screenshot harness, and the probe kit under it.** Jorge, 19 September: make the recommendations so they assist future builds. Every phone check on 18 September was a throwaway probe in a scratchpad: sign in on the mock, prepare the floor, swipe, screenshot, diff. `scripts/probe-kit.mjs` holds the sign-in, the floor prep and a thumb; `scripts/shots.mjs` walks Home, Live Floor, Phone Line and a mid-swipe at the three text sizes and writes the pictures; the checks workflow runs it in WebKit on every pull request, uploads the pictures, and links them from the pull request. | Claude | `claude/shots` | `scripts/probe-kit.mjs`, `scripts/shots.mjs`, `.github/workflows/checks.yml`, `README.md`, `package.json` | done | #390 |
| C68 | **Two more routines: the deploy watch and the TestFlight feedback pull.** Production sat broken for forty minutes on 18 September and nothing said so; Jorge's screenshots arrive by chat and get fixed one at a time against rule 5a. One routine reads Vercel's latest production deployment every hour and speaks only when it errored; one reads TestFlight feedback each morning and puts what came in on the board as one row. Prompts in `docs/routines.md`, word for word. | Claude | `claude/routines` | `docs/routines.md`, `README.md` | done | #391 |
| C69 | **The proposal page as a skill.** Every proposal and sheet so far was a hand-copied stylesheet. `.claude/skills/proposal-page/` holds the house page, the decision buttons and the copy button, so the next proposal is written from it rather than from the last one. | Claude | `claude/proposal-skill` | `.claude/skills/proposal-page/` | done | #392 |
| C70 | **The Live Activity rendered on a pull request.** The card took nine builds because nothing here can draw SwiftUI. A macOS job that renders the widget's states to PNG on a pull request touching `native/` and posts them. Cannot be compiled or run in the web session; each try is a blind round of CI, so it goes up behind a label and its first runs are the test of it. | Claude | `claude/activity-render` | `.github/workflows/sage-app.yml`, `native/targets/queue/` | done | #393 |
| C71 | **Findings from the render, 19 September: the floor up with a phone lane under it is 164 pt.** The first thing the Mac render (C70, #393) measured. The sheet's D1 arithmetic put You're up over a one-line phone lane at about 159; drawn on the simulator at 3x it is 164, four over the lock screen's 160. Every other state is inside: floor waiting 121, up alone 116, both quiet 159, the phone offer over the floor 156. Not fixed here: a card that needs the sheet to change is a proposal first, and this is one row to batch with whatever else the render finds, per rule 5a. | - | - | `native/targets/queue/QueueActivity.swift`, `docs/sheets/live-activity.html` | open | - |
| C72 | **Findings from the phone on the C29 preview, 19 September: the spine and the ground.** Jorge, on the #389 preview: the swipes make sense; the spine glitch and the background are still weird. Both were reported on 18 September too (Live Floor to Home: the corner's spine extended off the screen then snapped back; the ground one colour for a moment, then another), and #385 aimed at the timing of the old stage slide, which C29 removed. Probed on the C29 build in Chromium at Normal and Largest, sampling every frame through a tap each way and a swipe: the spine's box stays inside its pane on every sample (about 500 a move), and the ground blends through 10 to 16 colours with no jump. So neither shows in Chromium, and the WebKit runs cannot show a transient. The one lead for the spine: it is `position:fixed` inside a box that is transformed while it travels, which WebKit is known to lay out against the viewport mid-transition and then correct, and that was equally true of the old stage slide. Jorge sent a recording the same night, read frame by frame. It is of the production build, not the preview (the corner slides in over empty ground; the floor does not leave to the right), and it shows both. The spine: fixed inside a box the compositor moves, laid against the screen mid-travel, its rail running to the bottom edge with no head (2.10 to 2.17 s). Fixed in #389: the corner's lights and spine are drawn into the pane as absolute boxes. The ground: the blend runs 300 ms, holds, jumps at the end, in both directions. I first said the main thread was blocked by the corner's mount, and that was wrong: the recording after #389 (19 September, the site) shows the same step with the corner mounted once. The paint drew the leaving room's light at full under the arriving one, two lights just before the end and one at the end. It fades out as the other comes now, and a feel row holds the biggest change between two frames of the blend to 24 (98 before, 12 after). The spine is confirmed on the site in that recording. | Claude | `claude/ground-step` | `src/LeadPerformanceCalculator.jsx`, `docs/sheets/rooms.html`, `scripts/feel.mjs`, `test/feel.test.mjs` | done | #394 |
| C73 | **A preview cannot sign a salesperson in.** Found 19 September when Jorge could not sign into the C29 preview. Read against the project's own settings on 21 September, and two things were wrong with the row as written. **One: it is one variable, not two.** `api/_env.mjs` falls back, `SUPABASE_URL || VITE_SUPABASE_URL`, and `VITE_SUPABASE_URL` is already on Preview, as is `VITE_SUPABASE_ANON_KEY`. The only thing missing is `SUPABASE_SERVICE_ROLE_KEY`, which has no fallback on purpose, because a `VITE_` prefix would compile a key that bypasses every row-level policy into a file anybody can download. That is why the preview died with "supabaseKey is required" while the URL resolved fine. **Two: the project has no deployment protection at all.** `passwordProtection`, `ssoProtection` and `trustedIps` are all disabled, so every preview URL is reachable by anybody holding the link. Putting the service-role key on Preview as things stand would put a key that bypasses every policy, against the live database, behind a public URL on every branch including half-written ones. So the order matters: turn Vercel Authentication on for Preview deployments first, then tick Preview on the one variable. Jorge's call, and the trade-off is that a preview then has to be opened in a browser signed into Vercel. | Claude | - | `docs/routines.md`, `README.md` | needs Jorge | - |
| C74 | **Findings from the phone after #394, and how smooth the swipe can get.** Jorge, 19 September, third recording: no flash on the switch any more; a little flash as the app opens; a seam between Home and Live Floor; the swipe not as smooth as it could be, research how smooth it can get. Read to the frame: the opening flash is the native splash (#15211B, the mark) handing to the site's green curtain (a gradient from #7FA98A) at 1.15 to 1.18 s, two curtains of two colours; the seam is a one-pixel hairline where the two panes meet at a fractional offset (the thumb's clientX is not whole) plus the corner's own lights cut hard at the pane's edge while it travels; the swipe moves 3 to 6 px a frame with no reversals but 23 stalled frames in 182, each followed by a doubled step, which is the main thread missing a frame about one time in eight. In Chromium the ground is not the cost (measured with and without it); the loop itself is: touch, React render of the whole floor page, style, composite. The way past that is the browser's own scroller (a horizontal snap page for the pair, moved off the main thread at the display's rate), or at least writing the drag straight to the DOM. Proposal page with one decision per item: https://claude.ai/artifact/GCDt8ZupvGUVH8stSCZELc. Decided 19 September: A1 the phone's scroller, A2 whole pixels and a one-pixel bleed, A3 a fade at the edges, A4 a launch screen that goes green. | Claude | `claude/swipe-research` | `src/LeadPerformanceCalculator.jsx`, `scripts/feel.mjs`, `test/feel.test.mjs`, `native/app.json`, `native/assets/`, `docs/sheets/rooms.html` | done | #395 |
| C75 | **Findings from the phone after #395: Live Floor to Home arrives in pieces.** Jorge, 19 September, fourth recording, on the merged C74 build: tapping Home from Live Floor is very laggy. Read to the frame: the tap is answered within 48 ms (the phone's own INP rows for the bar's tabs on build 71664ad: 40 and 48 ms) and all three travels are a clean 20-frame ease-out with no stall, but the Home pane arrives as a skeleton. The flat cards come with the pane; the LED digits, the calendar's lit dots, the Today glyphs and the Points block land two to four frames later, top to bottom. That is WebKit painting the off-screen pane only as the scroller brings it in. The corner's entrance animation is not it (arrived is fixed at mount). Fix: each pane its own compositing layer, which WebKit keeps painted off to the side. Only a phone can confirm it. | Claude | `claude/pane-paint` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs` | done | #396 |
| C76 | **Findings from the phone after #395, second recording: the pill, the ground and the profile mark.** Jorge, 19 September, fifth recording, on the merged C74 build: the seam is better; the profile mark slides differently from the page; the pill glitches on a fast swipe. Read to the frame: the phone reports the scroller's position about a tenth of a second after the panes have moved (a tap: the pill glides on its own easing toward the tab, then snaps back to the late report and follows it; a swipe: the pill trails the panes by a hundred milliseconds and jumps at the end), Chromium shows none of it (the pill tracks the scroller to the pixel there), the pill has not followed a drag from the floor toward the line since C74 (the render preferred the scroller's report), and the profile mark is a fixed element the moving pane leaves behind. Fix, one pass with C75: the ramp and the thumb report the position themselves and the late reports are ignored while they drive; on a lift the pill and the ground finish to the page the phone will snap to, guessed by the phone's own momentum rule, and the settled report corrects a wrong guess; the pill follows a live drag first; the profile mark rides in the pane with the spine and the lights. Only a phone can confirm it. | Claude | `claude/pane-paint` | `src/LeadPerformanceCalculator.jsx`, `test/feel.test.mjs`, `docs/sheets/rooms.html` | done | #396 |
| C77 | **Findings from the phone after #396: the pill after a swipe to the line.** Jorge, 19 and 20 September, sixth and seventh recordings, on build ec7fde1. The seventh: every travel right, and at the very end, after a swipe from the floor to the line, the pill parked between Home and Live Floor with Phone Line lit; the sixth ended the same way. Read to the frame and the code: two writers had the pill. React writes it from the rooms; the thumb and the ramp write the same style straight to the element. The floor page on its way out under the rooms' cross still scrolls as it is hidden, and its scroll event reported a position that landed on the pill after the line was the room, and React did not write it back because its own value had not changed. Fix: the floor page's scroller says nothing unless the floor is the room (no report, no settle, no thumb), React re-asserts the pill whenever the rooms change, and the thumb stands down off the end of the page so the pill follows the rooms' drag. The sixth recording's pill running ahead on a flick did not recur in the seventh; a trace of each swipe stays in for one build in case it does. | Claude | `claude/swipe-trace` | `src/LeadPerformanceCalculator.jsx`, `api/_report.mjs` | done | #397 |
| C78 | **The board sheet, worth looking at.** Jorge, 20 September, on the merged C77 build: everything looks great; the leaderboard should be more visually appealing. Today: thirteen identical rows, a fat grey pill each, one gold, names cut off. Proposal with one decision per item: B1 the top three as a podium then the rest, B2 rows with initials, full name, a thin line under the name and the number on the right, B3 your own row pinned while out of view, B4 ties share a place, B5 the leader's line and the gap to first, B6 a caption saying units this month; decided 20 September: B1 podium then rows, B2 thin line under the name, B3 pin it, B4 yes, B5 and B6 leave, F1 yes. F1 rides along: the trace from the phone says iOS cancels the touch after eight points on some swipes and its snap takes 600 to 700 ms, so after a lift the pill and the ground should follow the phone's reports instead of racing them on the wipe's clock; the trace comes out in the same pass. Proposal page with one decision per item: https://claude.ai/artifact/2ecMDsbUoPZhoZDbgKoGQK | Claude | `claude/board-look` | `src/LeadPerformanceCalculator.jsx`, `api/_report.mjs`, `test/feel.test.mjs`, `docs/sheets/rooms.html` | done | #398 |
| C79 | **WebKit spends 80 to 110 ms a frame for half a second after a status tap.** Found under C66, 21 September, on the CI runner: tap Lunch or Here on the Live Floor and the next eight or so frames run 50 to 140 ms where Chromium runs a flat 17, for about half a second, on every tap and not only the first. Nothing owns it. Hidden one at a time and measured four taps each, the corner pane, the spine, the room bar and the rail all leave the median worst frame between 99 and 111 ms, which is the unmodified screen. Ruled out the same way: the pips' width and height, the pips' transitions, the segment pill's transitions, mcGrow, every animation under `.lpc`, the room bar's backdrop blur, and the rail's five travelling dots. So it is not one rule, it is a screen that is expensive for WebKit to draw: about 600 nodes in the floor room, two canvases, backdrop filters and a lot of gradients. **Not to be chased further from CI.** A shared runner is not a phone, and the next honest measurement is a Safari timeline from Jorge's own device, which would say in five minutes what another day of probes would not. Ask for that before spending anything here. | - | - | `src/LeadPerformanceCalculator.jsx` | open | - |
| C80 | **Vercel function storage is at 27.74 GB against 10 included.** Jorge's screenshot, 21 September: 27 GB on 24 August, about 66 by 5 September, pruned back to 27.74 since. Every preview deployment keeps its own copy of the ten `api/` routes, and the project deploys on every push to every branch, most of which cannot change the site: the C66 probe work alone pushed ten times, all under `scripts/` and `.github/`. #402 stops those deploying and takes 3 MB off the ingest function, which carried all seven of pdfjs's built files to import two. It reclaims nothing already stored: that needs a deployment retention policy or old previews deleted by hand, which is Jorge's call and is written up on the pull request. | Claude | - | `vercel.json`, `test/feel.test.mjs` | done | #402 |
| C60 | **#373's own fix does not survive a reload that also lands on a stale chunk.** Two more rows of "Failed to fetch dynamically imported module", 18 September at 20:27, two seconds apart, same store, two different builds. `BoardBoundary` reloads once on catch and clears its `lpcf:board-reloaded` guard only when `managerChunk()` actually resolves. When the deploy that broke the first load is still rolling out, the reload's own fetch lands on the next build's not-yet-live chunk, the guard is still set from the first failure, and nothing ever remounts `BoardScreen` again to try a third time: the boundary's `render()` returns `null` forever and the kiosk sits blank until somebody walks up to it. Fix: the guard becomes a timestamp instead of a flag, so a catch inside the cooldown schedules one delayed retry rather than giving up. Filed by the hourly error watch. | Claude | `claude/errfix-94799b9b` | `src/LeadPerformanceCalculator.jsx` | done | #383 |
| C5 | **Review the `--dvh` and safe-area division inside the text-size zoom.** Merged in #334. One rule redefines `--dvh`, `--sat` and `--sab` inside the four zoom roots so the phone's own measurements are not enlarged along with the words, and it carries every full-height salesperson screen. Verified by measurement and pixel diff in Chromium only, so WebKit is the open question, along with whether `--satx` can leak to the manager and two hand-tuned constants no test holds. The brief is the issue. **Jorge asked on 16 September for Codex to finish this.** Nudged on the issue and in the handoff (H-C4). The brief was refreshed at the same time: X1 is merged so the LF blocker is gone, and three of its five points moved under it (#347 changed the help button the `66px` constant was derived from and added two more container queries, #348 and #351 added two new WebKit surfaces). One loose end closed: `.q-page`'s `min-height:100vh` does not overhang in Chromium, because `vh` self-corrects under zoom where `dvh` did not, which is itself a WebKit question. | Codex | - | review only, no branch | open | #335 |
| C9 | **"Short by 126 at this pace" is not a pace figure.** `storePace.short` is `goal.bar - totalUnits`, the plain gap still to sell. The line calls it "at this pace", which is a projection. Holler Ford: goal 200, sold 74, so it says short by 126, while at this pace the store lands on 159 and is 41 short. Either the words or the number is wrong, and `needPerDay` uses `short` correctly, so it is the words. Copy, so it needs Jorge. | Claude | Claude | `claude/short-at-pace` | done | #344 |
| C12 | **The manager's sticky header is see-through on a phone.** `.topstack` is `position:sticky` with no background at all. On the desk `.topbar` happens to fill it, so nothing shows. On a phone the stack is 100px tall, `.topbar` covers the top 58 and `.sect-strip` the bottom 33 at 14% opacity, so the hero scrolls straight through a 9px band and through the tab pills themselves. `.topstack::after` paints a 20px white fade under the whole thing, which on the green hero reads as the white line that never goes away. Mobile only: on the desk the stack is 62px and the bar covers all of it. | Claude | `claude/mobile-site-optimization-qvei7u` | `src/Manager.jsx` | done | #343 |
| C6 | **Move the restore point out of the store row.** Every save currently ships a copy of the state it is replacing. Putting the restore point in a key of its own makes a save smaller and a store row easier to read. Noted in the September audit and deliberately not taken then, because it is bigger than the rest of that batch. | - | Claude | `claude/restore-point` | done | #346 |
| C13 | **The sold line runs through its own caption.** `.mc-tl` is `position:absolute; bottom:-14px` with three parts on one row, so when it wraps the block grows UPWARD into the chart. Measured at 390px with a real caption: Normal 1 line and clear, **Large 2 lines and 11.9px into the chart, Largest 3 lines and 13.8px**. A 360px phone hits it at Normal. Needs Jorge's pick between stacking, shortening and shrinking. | Claude | - | `claude/phone-caption-corner` | done | #347 |
| C14 | **Fold the help icon into the initials, and put the initials on the spine's axis.** The sheet behind the initials already has two rows that open the same help panel, so the question mark is a second door to a room you can already enter. Reverses an earlier decision still recorded in a code comment (consistency pass, item 8), which is flagged on the proposal rather than quietly dropped. The spine's centre line is 18px from the edge; the initials and the question mark sit on two other axes. | Claude | - | `claude/phone-caption-corner` | done | #347 |
| C15 | **The ground does not travel between rooms.** The rooms already slide, dim and sheen; the background is swapped instantly underneath. Home `#15211B`, Live Floor `#070A08`, Phone Line `#06090F`. Proposal is to crossfade the ground over the same `--t-wipe` as the slide, with a louder option where the ground leads. Approved 16 September: the arriving room's colour leads, bleeding in ahead of the room. | Claude | Claude | `claude/ground-leads` | done | #348 |
| C16 | **Swipe to change rooms.** The switch is already directional (`--ar-dx`, `arPageIn`/`arPageOut`); only the gesture is missing, and nothing in the app listens for one. Two shapes offered: follows the thumb, or a flick reusing the existing switch. Three things to respect: sideways scrollers inside rooms must win, iOS edge-back must be left alone, and a gesture that fights a scroll is worse than none. Do last, on its own. Approved 16 September: follows the thumb and settles either way, the bigger of the two shapes. Done as #351, thumb-following where there are two sheets. Home and Live Floor are two tabs of ONE `.q-page`, so that pair commits on release; making it uniform needs the corner and the floor rendered as two sheets, which is its own job. **It cannot use pointer events**: the app captures the pointer on every press, and the cancel arrives before the first move. Touch events only. | Claude | Claude | `claude/swipe-rooms` | done | #351 |
| C17 | **The marks came off the drawn figures, and the top three grew.** Done as #349: the verdict glyph is gone from the hero's channel bars, the video dials and both shapes of the desk table, because the tube, the dial and the bar already say it. The direction arrow stays, which is the one thing the drawing does not say. The podium card went 52px to 78px, the medal 26px to 38px, and the places are pixel numerals. Nothing else in that area moved, which Jorge asked for twice. Reverses part of the five-second pass item 5; that guard is rewritten, not deleted. | Claude | `claude/quieter-cards` | `src/Manager.jsx` | done | #349 |
| C18 | **The round-up chip spilled out of its card.** Done as #352. Not the window: the cards are a fixed 201px from 1280 to 2560. It is how many cards share the row. A store with yesterday's sold split gets two more, New and Used, and five columns of `minmax(108px, 1fr)` collapse from 201px to 112px, at which point the tube, the figure and the chip stop fitting. Measured 3px outside before, 69px inside after. **The demo store has no sold split, so the mock only ever draws three cards and this is invisible in it** which is why two reproductions came back clean. Worth remembering for the next one of these. | Claude | Claude | `claude/roundup-spill` | done | #352 |
| C19 | **The casted TV board (`?board=`) has no error boundary.** `app_errors` caught one row: "Failed to fetch dynamically imported module" for `Manager-<hash>.js` on `/?board=driver-s-mart-winter-park`. That route renders `BoardScreen`, a lazy chunk of `Manager.jsx`, with a bare `React.Suspense` and nothing to catch a rejected import, so a TV that has been up since the last deploy hits a white screen instead of the self-heal the code's own comment promises. `QueueBoard` already has `useBuildWatchdog()` for the same class of screen; this one has nothing. Fix: a boundary that reloads once on catch, guarded by a `sessionStorage` flag so a real (non-stale-chunk) error does not loop. Filed by the hourly error watch. | Claude | `claude/errfix-8b8530c3` | `src/LeadPerformanceCalculator.jsx` | done | #350 |
| C50 | **#350's own fix stops healing after its first use.** Two more rows of the same "Failed to fetch dynamically imported module" fault, 18 September, three minutes apart, same store, same build. `BoardBoundary`'s `sessionStorage.setItem("lpcf:board-reloaded", "1")` is never removed, unlike `main.jsx`'s `sage:swapped`, which is read and cleared in the same tick. A kiosk tab that self-heals once from a deploy never heals again for the rest of its life, because the flag it set to stop a reload loop outlives the reload it was guarding. Fix: clear the flag once `managerChunk()` actually resolves, so the guard only spans one failed attempt rather than the tab's whole lifetime. Filed by the hourly error watch. | Claude | `claude/errfix-f6c351ae` | `src/LeadPerformanceCalculator.jsx` | done | #373 |

## Settled

**The reports outage, 12 to 15 September.** Nine of ten stores stopped filing after 12 September and it ran for three days: the reports for the 13th and the 14th never arrived, and the 15th's did once it was fixed.

Corrected 15 September: this first said the outage began "at about 15:10 ET on 12 September", read off the last time those store rows were written. That was wrong about the schedule. Reports arrive in the MORNING, which the restored run showed plainly, all ten stores between 09:01 and 09:34 ET. So the 12th's reports had already filed that morning, the URL broke early that afternoon, and the first delivery actually lost was the morning batch of the 13th. The cause was `lpc-mail`'s `INGEST_URL` pointing at a Vercel deployment URL rather than the site's domain: Vercel removed that deployment and every report since was posted to a 404 and dropped. Nothing else showed it, because a 404 never reaches the function and so never reaches `app_errors`, and the endpoint itself tested healthy throughout. Jorge repointed both workers at `https://www.sageonline.io/...` on 15 September and stores began filing again the same morning. The morning brief now checks for silence first, so a repeat is a one-morning outage rather than a three-day one.

Worth knowing for next time: these must never point at a `*-<hash>-<org>.vercel.app` URL. Those are per-deployment and Vercel collects them. Only the domain is stable.

**The scanlines on the round-up head.** The white hairlines across the manager round-up's green head are `.s2-hero::before`'s scanline texture, a white line every three points at three percent, and they are on the dashboard hero card at the same strength. Measured on a flat patch of each at three times scale, with the layer switched off and on: 5.73 luminance steps on the round-up against 5.25 on the hero. They read harder on the round-up only because its head is 102 points tall against the hero's 373, so the same three-stop gradient is compressed and runs darker under them.

Jorge decided on 15 September that they stay: they are part of the character of the site. **Not to be proposed again, and not to be tidied away by anybody who reads the round-up head as a rendering fault.** It is deliberate.

Two dead ends, recorded so nobody walks them twice. The heavy `.s2-noise` layer, which draws the same lines at twenty two percent, is not involved: it sits at `opacity:0` unless the hero is in its grey lost-signal state. And the rounded corner is not leaking the sheet's own background: painting `.ru-sheet` red produces no red at the corner, the clip is clean. Also worth knowing: there are two round-ups, one in `Manager.jsx` and one on the salesperson side, and a question about "the round-up" needs to say which.

**For whoever takes the Windows checks (`X1`).** #334 added seven more source guards to `test/feel.test.mjs`, and several of them match across a line break, for example `` /@container mchead \(max-width:300px\)\{\n  \.mc-corner\{ flex-direction:row;/ ``. They are written against LF and they will fail on a CRLF checkout, which is the thing that item is fixing. They are not a new kind of problem, the file already had them at lines 143, 168, 169, 202 and 222, but they are new instances and they landed after that claim was made. Rebase on `main` before measuring, or the count will be short.

Two claims were numbered A7 on 15 September, by two agents within two minutes of each other. That is what the owner prefixes at the top of this file exist to stop. Worth knowing that the board's ids are not a reliable lock on their own: the row is.

**The 13th and the 14th of September are gone.** The outage cost nine of ten
stores two morning batches, and the reports for those two days were never
resent. Jorge decided on 15 September to live with them as lost rather than
chase a backfill from DriveCentric. So any figure covering that window is short
for nine stores, and it is short on purpose: nobody should go looking for a bug
in it, and nobody should propose the backfill again without Jorge raising it
first.

**Running the stock split backfill (#342).** Written, merged, and not run.
Nothing changes for a month already filed until somebody runs it with live
credentials, which no web session has. The order that makes sense:

```
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... \
  node scripts/backfill-stock-split.mjs --store holler-ford --month 2026-09
```

a dry run on one store first, check the new and used it prints against the PDF
for that store, then the same line with `--write`, then drop the filters for the
other nine. It says what it would do and writes nothing until told.

Worth knowing: only 31 August onwards can be recovered, because that is as far
back as the stored reports go. Anything earlier keeps the estimate, and there is
no way to get it back.

## Outside the repo, for Jorge

These are not agent work. They are here so nobody proposes them again.

| # | Item | Status |
|---|------|--------|
| J1 | Delete the empty "sage" Expo project on the jorgeasenriv team, then retry the transfer. | waiting on Jorge |
| J2 | Delete the Vercel project `lead-performance-calculator-srnl`. | done 14 September |
| J3 | Have a word with a salesperson about the PIN coming out of sign-in (#326). | done 15 September |
| J4 | **Run the stock-split backfill.** `node scripts/backfill-stock-split.mjs` says what it would do and changes nothing; `--write` does it. It needs `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` for the real project, which no agent has and none should. Ten files to read, one per store per month. Jorge asked to be reminded, so raise it at the start of every session until it is done. **A reminder is scheduled for 17 September, 9am ET**, which Jorge asked for on the 16th (routine `trig_015a4gDTXxKSWAmxNJLxeL82`, one shot, push and email). It carries no connectors, so that session cannot read Supabase to check whether it has already been run; it is told to say so rather than guess. | waiting on Jorge |
| J5 | **The Expo plan.** Upgraded by Jorge on 15 September, and proved rather than assumed: #340 got a real iOS check (build 32, four and a half minutes, not the 62-second skip that means "could not check") and merged. | done 15 September |

## Done

| PR | Item | Owner |
|----|------|-------|
| #326 | The PIN stage removed from sign-in. Picking a name is the whole of it. | Claude |
| #327 | One way to say two names are one person: one sentence, one audit line. | Claude |
| #328 | Printing opens one window, and the sign-in poster takes the room. | Claude |
| #329 | The two-tap check measures from inside the page and reads the server until it settles. | Claude |
| #331 | One selector drew the 9px verdict glyphs at 68 by 34, and three sizings fell out of it. | Claude |
| #332 | One line for the labels, and a morning brief that notices silence. | Claude |
| #333 | There was no third write: the settle rule was reading a chain mid flight. | Claude |
| #334 | The screen's own measurements are not text: the text size is a zoom, and it was enlarging the phone's height and its safe areas along with the words. | Claude |
| #337 | The Online room's under construction page says what a manager gets out of it, in one line, instead of three bullets about the plumbing. | Claude |
| #338 | The Online hero is a sign and a sentence, clear of the header by the 64px the other pages leave. The tape on a phone had been 0px tall since it was written. | Claude |
| #339 | `public.queue_identity` dropped, the PIN's table, with its six rows. Nothing had read it since #326. | Claude |
| #341 | New and used are counted off the report's own rows instead of scaled from the people's, so the stock split is whole cars. | Claude |
| #342 | A backfill for the stock split on months already filed, dry run by default. Written and tested, **not yet run against production**: `scripts/backfill-stock-split.mjs`. | Claude |
| #350 | The casted TV board (`?board=`) hit a stale chunk after a deploy and crashed to white. `BoardBoundary` catches it and reloads once. | Claude |
| #353 | Three from a real phone on build 33: the sold line's caption now measures itself and shrinks to fit instead of trusting a coefficient calibrated against the wrong worst case, the weekday sits beside the calendar again at Normal (the 300px breakpoint was borrowed, 250px is measured), and a swipe buzzes when the room lands while a button still buzzes at once. | Claude |
| #355 | The Live Activity's cord runs level with the handset it ends at, and gets back the scaling it never actually had: the divisor was always 1, which #354 turned from harmless into a curve hanging below its own box. Mine, found from a lock screen photo, never shipped. | Claude |
| #360 | The corner goes back to green. The warm sand of #357 sat behind the corner's own four lights, three of them green, and read as two ideas at once. | Claude |
| #361 | The backdrop was 232.8 MB of GPU texture across seven moving layers, which is what a WKWebView cannot hold and why the phone stuttered. One canvas, 1.26 MB, pixel for pixel the same effect. My first cut of it was slower than what it replaced, and the reason was the backing resolution rather than either of the two things I fixed first. | Claude |
| #362 | Three from a photograph of the phone. The flashing between pages was not the canvas: #359's cross painted an opaque slab on both sheets for 440 ms and then snapped, and the leaving sheet is cut to the arriving one's edge instead. The dots were soft because I drew them at one backing pixel and said it would not show; they have their own canvas at the screen's resolution. The seam was the parked loading curtain's 60px shadow landing on the first 60px of the room in front of it, which during a drag is the join: the same fault as #356's, in a second place. I reported the seam as the canvas's first, and it was the rooms'. | Claude |
| #363 | No light crosses the rooms on a switch. The sheen riding the arriving sheet said the room moved a second time, in the language of a wipe, and the backdrop already says it in the language of depth. | Claude |
| #364 | The corner's elements arrive from the right rather than lifting, which off the Live Floor is what actually happened to them. Sunlight is removed entirely: the switch, the boot effect, the preference, the glyph and twenty-two rules, with the two backdrop rules it scoped now unscoped instead of deleted. Found on the way: `mcRise` was declared twice and the later one won for both, so the corner had been lifting 16px rather than its own 14. | Claude |
| #365 | A new build is taken at the next open rather than the one after. The worker only let one in on the way to the background, so the reload happened off-screen and the build a person saw was always the build before last: two changes were reported as not working while being correct and live. Force quitting made it worse, not better. Verified end to end by swapping the server between two builds. The month also goes on a card, because the backdrop's dots were being read against the calendar's. | Claude |
| #366 | Five from a phone. The question mark off the Phone Line, the two rooms' words at one height, the corner landing rather than appearing with the spine landing too, a swipe finishing where the thumb left it, and the first crossing no longer showing its loading skeleton. The release throwing away the drag's position was one fault behind two of the five: it snapped the pages back and, in the same frame, put the leaving title inside the cut. | Claude |
| #368 | One motion for a tap and a swipe: a tap was a push that covered the sheet being left, which is what folding looks like, and both tile now. The push, its two sets of cut keyframes and #359's flat slab are all deleted. The app opens at Home. Home and Live Floor travel, one-sidedly, which is a step towards C29 rather than C29. A guard caught `tabMove` colliding with Manager.jsx's own. | Claude |
| #369 | The first open after a deploy cost 3.5s against a steady 2.0, because #365 adopted the new build mid-open and `location.reload()` is held until the page it was called on finishes loading: a build taken at 266 ms did not commit until 1301 and threw the whole boot away. Adoption now happens only where it is free, at the start of an open before anything is drawn or on the way to the background. One open late at worst, never slow. Steady-state boot measured flat back to #353, so the app itself was never the problem. | Claude |
| #370 | The corner's cards no longer fade in on top of the stage's slide, which was two motions at once: the page 90 per cent off screen with its cards at half opacity, and the cards still settling half a second after the page had stopped. A corner arriving on the slide reads that once at birth and keeps its cards still; the cold open keeps its entrance. | Claude |
