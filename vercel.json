{
  "functions": {
    "api/ingest.mjs": {
      "includeFiles": "node_modules/pdfjs-dist/legacy/build/*.js"
    }
  },
  "headers": [
    {
      "source": "/sw.js",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, max-age=0, must-revalidate"
        },
        {
          "key": "Service-Worker-Allowed",
          "value": "/"
        }
      ]
    }
  ],
  "//": "ignoreCommand: a commit that touches only docs and markdown does not deploy. Every board claim and release commit deployed production on 18 September, and the Board's wall reloaded for each one. Exit 0 skips, as Vercel reads it.",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)docs' ':(exclude)*.md'"
}
