# Addresses allowed to reach the SFTP server, one per line (IPv4, or a
# range like 203.0.113.0/24). Empty means nobody from outside. PromptPath's
# go here when Chris sends them; then run the workflow with "firewall".
